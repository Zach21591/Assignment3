<nav class="navbar navbar-expand-lg navbar-black bg-white">
    <div class="container-fluid">
      <!--Adds a text with authors name on the left side of the navbar that takes them to the home page-->
      <a class="navbar-brand" href="/home"><i class="fa-solid fa-circle-user fa-lg"></i> Zach D'Angelo</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <!--The following if statement represent when a page is displayed, the link/button of the page is darker than the rest; When page is not displayed, regular color-->
          <li class="nav-item">
            <% if(title == "Home") { %>
              <a class="nav-link active" href="/">
            <% } else { %>
              <a class="nav-link" href="/">
            <% } %>
              <i class="fa-solid fa-house fa-lg"></i> Home</a>
          </li>
          <li class="nav-item">
            <% if(title == "About Me") { %>
              <a class="nav-link active" href="/about">
            <% } else { %>
              <a class="nav-link" href="/about">
            <% } %>
              <i class="fa-solid fa-address-card fa-lg"></i> About Me</a>
          </li>
          <li class="nav-item">
          <li class="nav-item">
            <% if(title == "Team List") { %>
              <a class="nav-link active" href="/teamlist">
            <% } else { %> 
              <a class="nav-link" href="/teamlist">
            <% } %>
            <i class="fa-solid fa-medal fa-lg"></i> Sports Teams</a>
          </li>
          <li class="nav-item">
            <% if(title == "Contact Me") { %>
              <a class="nav-link active" href="contacts">
            <% } else { %> 
              <a class="nav-link" href="contacts">
            <% } %>
              <i class="fa-solid fa-phone-flip"></i> Contact Me</a>
          </li>
          <% if (typeof displayName == 'string' && displayName){ %>
            <li class "navbar-text">
              <i class="fas fa-user"></i>
              <%= displayName %>
            </li>
            <li class="nav-item">
                <a href="/logout" class="nav-link">
                <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a>
          </li>
          <% } else { %>
            <li class="nav-item">
                <a href="/login" class="nav-link">
                <i class="fas fa-lg fa-sign-in-all"></i> Login</a>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>